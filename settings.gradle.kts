rootProject.name = "cyclohexane"

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven("https://maven.pkg.jetbrains.space/public/p/compose/dev")
        maven("https://artifactory.tcsbank.ru/artifactory/maven-all/")
    }
}
